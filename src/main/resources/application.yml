server:
  port: 5500

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/user_service"
    username: "postgres"
    password: "postgres"
  jpa:
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8082/realms/userservice
